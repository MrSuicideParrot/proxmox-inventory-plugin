namespace: mrsuicideparrot
name: proxmox_plugin
version: 0.0.1
readme: README.md
authors:
- André Cirne


description: Proxmox inventory source

# Either a single license or a list of licenses for content inside of a collection. Ansible Galaxy currently only
# accepts L(SPDX,https://spdx.org/licenses/) licenses. This key is mutually exclusive with 'license_file'
license:
- GPL-2.0-or-later

# The path to the license file for the collection. This path is relative to the root of the collection. This key is
# mutually exclusive with 'license'
license_file: ''

# A list of tags you want to associate with the collection for indexing/searching. A tag name has the same character
# requirements as 'namespace' and 'name'
tags: ['proxmox','ansible','inventory']

# The URL of the originating SCM repository
repository: https://github.com/MrSuicideParrot/proxmox-inventory-plugin

# The URL to any online docs
documentation: https://github.com/MrSuicideParrot/proxmox-inventory-plugin

# The URL to the collection issue tracker
issues: https://github.com/MrSuicideParrot/proxmox-inventory-plugin/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered
build_ignore: ['build/*', 'test/*']

